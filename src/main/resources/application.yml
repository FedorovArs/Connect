spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost/connect}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 504338490835-rcobmg5uhlputl3ui794mt4lk1beb268.apps.googleusercontent.com
            client-secret: ${clientSecretKey}